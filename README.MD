Spring Boot on JBoss Example
---

This is an example of how to configure a simple spring boot application to run on JBoss
Where a dispatcher servlet mapping of '/' doesn't work and requires a mapping of '/*'